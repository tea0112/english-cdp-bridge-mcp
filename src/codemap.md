# src/

Root of the Python package tree. Contains only the `cdp_bridge/` sub-package.

## Aggregation

`src/cdp_bridge/` — main package (see `src/cdp_bridge/codemap.md`)

```
src/
└── cdp_bridge/    # Python package (TMWebDriver, server, middleware, simphtml, chrome extension)
```
